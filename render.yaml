services:
  - type: worker
    name: cabane-bridge
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: MQTT_BROKER_URL
        value: mqtt://broker.hivemq.com:1883
      - key: GALLONS_PER_CYCLE
        value: "3"
